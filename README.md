# Robot Grasp Detection

This project explores robotic grasp detection using Pybullet for simulation

Most recent progress:
- Working on PyBullet simulation
- Next Step: integrate vision model (gr-convnet)
- future steps: DQN agent to learn grasp policy.

## How to Run
1. Install dependencies:
   ```bash
   pip install -r requirements.txt

``` bash
python sim/env.py
```
Requirements:
Python 3.12
PyBullet 3.2.7
NumPy, Math, Time